dictionary = { "some key": "some value" }
#the next line is optional and can be used as [  for key in dictionary:  ]
for key, value in  dictionary.items():
    print( "%s ---> %s" %(key, dictionary["some key"]))